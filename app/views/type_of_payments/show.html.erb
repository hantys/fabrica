<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @type_of_payment.name %>
</p>

<legend>Sub-Itens</legend>
<ul>
  <% @type_of_payment.sub_type_payments.each do |item| %>
    <li>
      <%= item.name %>
    </li>
  <% end %>
</ul>

<%= link_to 'Edit', edit_type_of_payment_path(@type_of_payment) %> |
<%= link_to 'Back', type_of_payments_path %>
