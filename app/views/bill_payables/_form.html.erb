
<%= simple_form_for(@bill_payable) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="form-inputs">
    <%= f.input :references %>
    <%= f.input :status %>
    <%= f.input :references %>
    <%= f.input :references %>
    <%= f.input :obs %>
    <%= f.input :file %>
    <%= f.input :total_value %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
