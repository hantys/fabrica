<%= simple_nested_form_for(@revenue) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>
  <% if @revenue.errors.full_messages.any? %>
    <ul>
      <% @revenue.errors.full_messages.each do |error_message| %>
        <li class="alert alert-danger">
          <%= error_message if @revenue.errors.full_messages.first == error_message %> <br />
        </li>
      <% end %>
    </ul>
  <% end %>
  <div class="row">
    <div class="col-lg-4">
      <%= f.input :name %>
      <%= f.association :category, prompt: "Escolha uma Categoria", input_html: { class: 'simple-select2' } %>
    </div>
  </div>
  <hr>
  <%= f.button :submit, class: 'btn btn-success' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")), revenues_path, class: 'btn btn-danger' %>
<% end %>
